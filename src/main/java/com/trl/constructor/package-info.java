/**
 * Classe hija en su constructor simpre por defecto llama al constructor de padre con palabra reservada "super()"
 * <p>
 * clase hija llama constructor por defecto(este constructor que crea compilador, sin paramthos) de la clase padre, si clase
 * padre no tiene definido constructor por defecto, clase hija tiene que llamar otro constructor que esta difinido en
 * la clase padre.
 */
package com.trl.constructor;