/**
 * Existen dos tipos de classes "Top level classes", y "Nested Classes".
 * <p>
 * "Top level clase" - es una clase normal que usamos simpre.
 * Se puede desir de otra forma, que "top level class es una clase que no esta realizada dentro de otra clase.
 * <p>
 * "Nested Class" - es la clase que se define dentro de una clase.
 * <p>
 * Existe un tipos de casse que se crean dentro de un calse, este tipo de clase se llama "Nested Class".
 * A su vez "Nested Class" se devide en dos sub tipos: Nested Static Class, Inner Class.
 * Tambien existe un tipo de classes que se llaman "anonymous class. "Anonymous class" es simpre de tipo "Nested Class".
 * <p>
 * <p>
 * Si dentro de un archvo se define una clase por ejemplo "public class Parent", este archivo esta obligado tener
 * mismo nombre que la clase.
 * <p>
 * Un archivo puede tener muchos classes defindos dentro, pero se permite definir solo una clase con modificador public.
 * <p>
 * Si tenemos un archvo con 5 clases definidos dentro. Despues de compilacion nosotros vamos a tener 5 archivos,
 * cada clase va tener su archvo propio.
 */

package com.trl.class_.class_nestedClass_nestedStaticClass_innerClass;